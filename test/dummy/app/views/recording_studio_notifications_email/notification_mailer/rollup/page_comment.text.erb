<%= pluralize(@events.size, "new comment") %>

<% @events.each do |event| %><%= event.title %>
<% if event.body.present? %><%= event.body %>
<% end %><% if event.url.present? %>Open page: <%= event.url %>
<% end %>
<% end %>